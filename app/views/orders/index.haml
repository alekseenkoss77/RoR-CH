= form_with url: orders_url, method: :get do |form|
  %row
    %col-md-3
      = form.label :query, "Search by:"
      = form.text_field :number, placeholder: "Number", value: params[:number]
      = form.text_field :email, placeholder: "Email", value: params[:email]
    = form.submit "Search"

%table.table
  %thead
    %tr
      %th Nuber
      %th State
      %th Email
      %th Created at
  %tbody
    = render partial: 'orders', locals: { orders: @orders }
